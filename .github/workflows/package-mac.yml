name: Build MacOS DMG

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4 # Version mise à jour

      - name: Set up JDK 17
        uses: actions/setup-java@v4 # Version mise à jour
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean package

      - name: Create DMG with jpackage
        run: |
          jpackage --type dmg \
            --dest target/dist \
            --name "GhibliTodo" \
            --main-jar app-1.0-SNAPSHOT.jar \
            --main-class com.ghibli.todolist.App \
            --module-path target/libs:target/app-1.0-SNAPSHOT.jar \
            --input target/ \
            --icon src/main/resources/background.icns || echo "Icon not found, continuing without it"

      - name: Upload DMG
        uses: actions/upload-artifact@v4 # Version mise à jour
        with:
          name: GhibliTodo-Mac-DMG
          path: target/dist/*.dmg
